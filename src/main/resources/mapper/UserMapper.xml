<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qingyuan.redtour.mapper.UserMapper">
    <resultMap id="user" type="com.qingyuan.redtour.pojo.User">
        <id column="user_id" property="userId" jdbcType="INTEGER"/>
        <result column="openid" property="openid" jdbcType="VARCHAR"/>
        <result column="user_name" property="username" jdbcType="VARCHAR"/>
        <result column="avater" property="avatarUrl" jdbcType="VARCHAR"/>
        <result column="credits" property="credits" jdbcType="INTEGER"/>
        <result column="identity" property="identity" jdbcType="INTEGER"/>
    </resultMap>

    <insert id="insertUser" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO user(openid) VALUE (#{openid,jdbcType=VARCHAR})
    </insert>

    <select id="getUserByOpenid" resultType="com.qingyuan.redtour.pojo.User">
        select user_id,openid,user_name,avatar,credits,identity
        from user
        where openid = (#{openid,jdbcType=VARCHAR})
    </select>


</mapper>
